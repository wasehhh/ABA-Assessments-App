# Platform docs

Placeholder for platform architecture contracts. Currently empty.

Contracts expected to move here after Alpha, **as candidates rather than a commitment**: `user_lifecycle_contract.md`, `user_profile_authorization_contract.md`, `cross_app_identity_contract.md`, and the platform portions of `security_and_roles.md`, `supabase_setup.md`, `database_schema.md`.

**Until they move they remain at `[repo] docs/architecture/` and are indexed by that directory's README.**

**No Cursor rule currently governs this directory.** Platform agent rules are the Platform SPM's to write.
