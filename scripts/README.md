# scripts/

`generate_test_pack.js` prints SQL that inserts a 20-domain, 100-target performance pack into `content_packs`.
Run: `node scripts/generate_test_pack.js`

`generate_json_file.js` writes the same pack as JSON to `tests/data/large_test_pack.json`.
Run: `node scripts/generate_json_file.js`
